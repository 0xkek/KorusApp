{
  "buildCommand": "npx expo export --platform web --output-dir web-build && cp web/phone-demo.html web-build/ && cp web/test-iframe.html web-build/ && cp public/_headers web-build/",
  "outputDirectory": "web-build",
  "rewrites": [
    {
      "source": "/",
      "destination": "/phone-demo.html"
    }
  ]
}