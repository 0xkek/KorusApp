{
  "buildCommand": "npx expo export --platform web --output-dir web-build && cp web/hackathon-landing.html web-build/ && cp -r pitch web-build/",
  "outputDirectory": "web-build",
  "rewrites": [
    {
      "source": "/",
      "destination": "/hackathon-landing.html"
    },
    {
      "source": "/app",
      "destination": "/index.html"
    }
  ]
}