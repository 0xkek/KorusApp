import React from 'react';
import { View } from 'react-native';

// This is a dummy component since the wallet tab will open a modal
export default function WalletTab() {
  return <View style={{ flex: 1 }} />;
}